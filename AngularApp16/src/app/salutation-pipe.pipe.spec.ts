import { SalutationPipePipe } from './salutation-pipe.pipe';

describe('SalutationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SalutationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
